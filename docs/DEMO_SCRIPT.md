# DEMO SCRIPT — 3 Minutes

## T+00s — Opening
"This is LoomiFlow AI — an Autonomous Commerce Operations Agent built on Loomi Connect."
Dashboard loads. Heartbeat pulsing gently (IDLE).

## T+20s — Context
"Most platforms handle payment failures reactively. We built a system that responds in
real-time using Bloomreach Loomi Connect MCP as its intelligence layer."

## T+40s — Scenario 1: Cart Abandonment (warm-up)
→ Click "Cart Abandonment" in Scenario Simulator
→ 3 agents activate, mild signal
→ Action Panel: personalized recovery message
"The system detects intent — not just behavior."

## T+70s — Scenario 2: Conversion Anomaly (intelligence)
→ Click "Conversion Anomaly"
→ Analytics MCP surfaces systemic pattern
→ execute_analytics → anomaly detected
"MCP analytics surfaces invisible patterns across the entire funnel."

## T+100s — FRAUD STORM (climax)
→ Click "🌩 FRAUD STORM"
→ Events flood in — heatmap fills red
→ 3 agents flash in parallel:
   FRAUD=BLOCK | REVENUE=ALLOW | CX=STEP-UP AUTH
→ Orchestrator resolves: STEP_UP_AUTH (87% confidence)
→ Heartbeat → CRITICAL, screen tint red
→ Traffic split: PROD 25% / CANARY 60% / SHADOW 15%
"Three agents disagree. The orchestrator resolves the conflict — and explains why."

## T+140s — VIP Payment Failure (show the full loop)
→ Click "VIP Payment Failure"
→ Memory graph shows: MCP tools → signals → decision
→ Action Panel: Bloomreach WRITE confirmation
→ "Email sent via Mailgun"
"Every decision is observable. Every action is traceable.
get_customer_prediction_score showed churn 82% — that's why we chose step-up auth over block."

## T+180s — Close
"Not a chatbot. An autonomous operations system — observable, explainable, resilient."
